# Скрипты для работы с базой данных

## 🔑 Установка/обновление пароля пользователя

### Способ 1: Через Node.js скрипт (РЕКОМЕНДУЕТСЯ)

Самый простой способ - использовать готовый скрипт:

```bash
cd back
node scripts/updatePassword.js <новый_пароль>
```

**Пример:**
```bash
node scripts/updatePassword.js myNewPassword123
```

**Важно:** Email захардкожен в системе как `hellonm4@mail.ru`, поэтому передавать его не нужно - только пароль!

Скрипт автоматически:
- ✅ Подключится к базе данных
- ✅ Найдет пользователя с email `hellonm4@mail.ru` (или создаст нового, если его нет)
- ✅ Захеширует новый пароль через bcrypt (10 раундов)
- ✅ Установит/обновит пароль в БД

---

### Способ 2: Через MongoDB Compass (GUI)

1. Скачайте [MongoDB Compass](https://www.mongodb.com/try/download/compass)
2. Подключитесь к вашей БД используя строку подключения:
   ```
   mongodb://mongo:UBRfgmnfYKgFXZVVgfpHcfEZQwYAoThG@crossover.proxy.rlwy.net:15075
   ```
3. Найдите коллекцию `users`
4. Найдите документ с нужным email
5. **НО:** Пароль должен быть захеширован через bcrypt! Просто текст не подойдет.

⚠️ **Проблема:** Вручную захешировать bcrypt сложно, поэтому лучше использовать скрипт.

---

### Способ 3: Через mongo shell

Если у вас установлен MongoDB shell:

```bash
mongosh "mongodb://mongo:UBRfgmnfYKgFXZVVgfpHcfEZQwYAoThG@crossover.proxy.rlwy.net:15075"

# В shell:
use <название_базы>
db.users.updateOne(
  { email: "hellonm4@mail.ru" },
  { $set: { password: "$2a$10$..." } }  # Нужен захешированный пароль!
)
```

⚠️ **Проблема:** Нужно сначала захешировать пароль через bcrypt, что сложно сделать вручную.

---

## ✅ Рекомендация

**Используйте скрипт** - это самый простой и безопасный способ:
```bash
node scripts/updatePassword.js новый_пароль
```

**Примечание:** В вашей системе нет логина - только пароль. Email всегда `hellonm4@mail.ru` (захардкожен в коде).

