# Настройка бесплатной базы данных MongoDB

## 🚀 Вариант 1: Railway.app

### Шаги:

1. Зайдите на [railway.app](https://railway.app) и зарегистрируйтесь через GitHub
2. Нажмите "New Project" → "Empty Project"
3. Нажмите "+ New" → выберите "Database" → "Add MongoDB"
4. Railway автоматически создаст MongoDB инстанс
5. Перейдите в раздел "Variables" вашего MongoDB сервиса
6. **ВАЖНО:** Найдите переменную `MONGO_URL` или `DATABASE_URL`
7. **Для локальной разработки:** Вам нужен **PUBLIC** (внешний) URL, а не internal!
    - Если видите `mongodb.railway.internal` - это внутренний URL (работает только на Railway)
    - Нужен URL вида `mongodb://mongo.railway.app:27017` или `mongodb+srv://...`
8. Если видите только internal URL, перейдите в раздел "Connect" → "Public Network" и включите его
9. Скопируйте **PUBLIC** строку подключения

### Настройка в проекте:

Создайте файл `.env` в папке `back/` (если его нет) и добавьте:

```
DATABASE_URL=ваша_строка_подключения_из_railway
SECRET_KEY=super-strong-secret
PORT=3000
```

Или просто замените строку в `utils/config.js`:

```javascript
const dataBaseAdress = "ваша_строка_подключения_из_railway";
```
