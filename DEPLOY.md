# 🚀 Инструкция по деплою на Vercel

## ✅ Что нужно сделать перед деплоем:

### 1. Настройка переменных окружения в Vercel

После деплоя бэкенда на Vercel, нужно настроить переменные окружения:

#### Для бэкенда (Backend):

В настройках проекта Vercel → Settings → Environment Variables добавьте:

```
DATABASE_URL=mongodb://mongo:UBRfgmnfYKgFXZVVgfpHcfEZQwYAoThG@crossover.proxy.rlwy.net:15075
SECRET_KEY=super-strong-secret
NODE_ENV=production
```

**Или** если у вас есть внутренний URL Railway (для продакшена):
```
DATABASE_URL=mongodb://mongo:UBRfgmnfYKgFXZVVgfpHcfEZQwYAoThG@mongodb.railway.internal:27017
SECRET_KEY=super-strong-secret
NODE_ENV=production
```

#### Для фронтенда (Frontend):

В настройках проекта Vercel → Settings → Environment Variables добавьте:

```
REACT_APP_MAIN_URL=https://ваш-бэкенд-url.vercel.app
```

Где `ваш-бэкенд-url.vercel.app` - это URL вашего задеплоенного бэкенда на Vercel.

---

## 📝 Шаги деплоя:

### Бэкенд:

1. Отправьте код на GitHub
2. В Vercel создайте новый проект
3. Подключите репозиторий
4. **Root Directory:** укажите `back/`
5. **Framework Preset:** Other
6. **Build Command:** оставьте пустым (или `npm install`)
7. **Output Directory:** оставьте пустым
8. **Install Command:** `npm install`
9. **Development Command:** оставьте пустым
10. Настройте переменные окружения (см. выше)
11. Deploy!

### Фронтенд:

1. В Vercel создайте еще один проект (или используйте тот же)
2. Подключите тот же репозиторий
3. **Root Directory:** укажите `front/`
4. **Framework Preset:** Create React App
5. Настройте переменную окружения `REACT_APP_MAIN_URL` (см. выше)
6. Deploy!

---

## ⚠️ Важно:

1. **URL бэкенда:** После деплоя бэкенда скопируйте его URL и используйте в `REACT_APP_MAIN_URL` для фронтенда

2. **CORS:** Убедитесь, что URL фронтенда добавлен в `back/middlewares/cors.js` в массив `allowedCors`

3. **Cookies:** Код уже настроен для работы с HTTPS (secure cookies для продакшена)

4. **База данных:** Используйте публичный URL для локальной разработки, внутренний URL Railway для продакшена (если бэкенд тоже на Railway)

---

## 🔍 Проверка после деплоя:

1. Откройте URL бэкенда - должно быть сообщение об ошибке 404 (это нормально, если нет корневого роута)
2. Откройте URL фронтенда - должна загрузиться страница логина
3. Попробуйте войти - должно работать!

---

## 🐛 Если что-то не работает:

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все переменные окружения настроены
3. Проверьте, что URL бэкенда правильный в `REACT_APP_MAIN_URL`
4. Проверьте CORS настройки

